<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:KMISApp.ViewModels"
             mc:Ignorable="d"
             x:Class="KMISApp.OfferDetailPage">
    <StackLayout Margin="30"
                 VerticalOptions="Center"
                 HorizontalOptions="Center">
        <Entry Placeholder="Escribe el correo de la cuenta"
               x:Name="emailEntry"
               Keyboard="Email"/>
        <Entry Placeholder="Escribe la clave de la cuenta"
               x:Name="passwordEntry"/>
        <Picker Title="Escoje el servicio ofertado"
                x:Name="servicePicker"
                ItemsSource="{Binding local:Servicios}"
                ItemDisplayBinding="{Binding ServiceName}">
        </Picker>
        <Button Margin="0,50,0,0"
                x:Name="updateButton"
                Clicked="updateButton_Clicked"
                Text="ACTUALIZAR"
                BackgroundColor="DarkSeaGreen"/>
        <Button Margin="0,30,0,0"
                x:Name="eraseButton"
                Clicked="eraseButton_Clicked"
                Text="BORRAR"
                BackgroundColor="Red"/>
    </StackLayout>
</ContentPage>